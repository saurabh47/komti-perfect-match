import { HeightFormatPipe } from './height-format.pipe';

describe('HeightFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new HeightFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
